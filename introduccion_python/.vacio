d['k1'][3]['tricky'][3]['target'][3]

dicti == tinydict

def dogo(s):
    if any(t == 'perro' for t in s.split()):
        return True
    else:
        return False	


def acumulate(li):
    for i in range(len(li)-1,0,-1):
        if i != 0:
            for j in range(i):
                li[i] += li[j]
    return li

li = [1,2,3,4]
acumulate(li)